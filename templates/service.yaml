apiVersion: v1
kind: Service
metadata: 
  name: nginx-LoadBalancer
  namespace: maxit
spec:
  type: {{ .Values.service.type }}
  selector:
    project: beta
  ports:
    - protocol: TCP  
      port: {{ .Values.servcie.servicePort }}
      targetPort: 80
      nodePort: 30007
  
